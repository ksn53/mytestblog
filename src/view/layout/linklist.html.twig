          <div class="p-3">
            <h4 class="font-italic">Ссылки</h4>
            <ol class="list-unstyled mb-0">
            {% for link in links %}
                <li><a href="{{ link[2] }}">{{ link[1] }}</a></li>
            {% endfor %}
            </ol>
          </div>