        <div class="p-3">
            <h4 class="font-italic">Страницы</h4>
            <ol class="list-unstyled mb-0">
            {% for page in pages %}
                <li><a href="http://{{ servername }}/page/{{ page[0] }}">{{ page[1] }}</a></li>
            {% endfor %}
            </ol>
        </div>